export type BlockType = 'tour' | 'food' | 'shop' | 'move' | 'stay';
export type Todo = { id: string; text: string; done: boolean; time?: string };
export type Reservation = { owner?: string; url?: string; number?: string; status?: string; capture?: string };
export type Travel = { fromId: string; mode: 'walk' | 'transit' | 'taxi'; minutes: number; text: string };
export type Hotel = { name: string; zhName?: string; zhAddress?: string; lng?: number; lat?: number };
export type Day = { id: string; n: number; date: string | null; city: string; title: string; moves: { kind: 'plane' | 'train' | 'hotel'; text: string }[]; hotel: Hotel | null; snacks: Snack[] };
export type Block = { id: string; day_id: string; position: number; type: BlockType; name: string; zh_name: string | null; zh_address: string | null; lng: number | null; lat: number | null; start_time: string | null; duration_text: string | null; duration_min: number | null; note: string | null; memo: string | null; important: boolean; todos: Todo[]; reservation: Reservation | null; travel: Travel | null; menu: MenuItem[]; updated_by: string | null; updated_at: string };
export type Expense = { id: string; day_id: string | null; block_id: string | null; label: string; category: string; amount: number; created_by: string | null; created_at: string };
export type Change = { id: string; day_n: number | null; summary: string; actor: string | null; created_at: string };
export type AiOption = { title: string; reason: string; summary: string; changes: { id: string; start_time?: string; duration_min?: number; remove?: boolean }[] };
export type MenuItem = { id: string; ko: string; zh?: string; price?: string; desc?: string; photo?: string; pick?: boolean; qty?: number };
export type Snack = { id: string; ko: string; brand?: string; zh?: string; delivery?: boolean; done?: boolean; by?: string };
